#include <iostream>
#include <vector>
using namespace std;
int main()
{
    // vector<int>v;
    // v.push_back(10);
    // v.push_back(34);
    // v.push_back(50);
    // for(auto i=v.begin();i !=v.begin();i++ )
  // cout<< *i;
  float x,y,z;
  cin>> x;
   y=(int)x;
cout << x-y;

} 